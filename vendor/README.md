# Vendored browser dependency

`supabase.min.js` is the pinned UMD build of `@supabase/supabase-js` 2.49.4.
It is served from the same origin and included in the PWA shell cache so an already signed-in learner can continue using cached courses and local progress during a temporary network outage.
